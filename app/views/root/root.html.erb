<%= link_to "Movies Index", movies_url %>

<%= link_to "Add Still", new_still_url %>
<%= link_to "Add Movie", new_movie_url %>

<form action="<%= search_url %>" method="get">
  <label for="search">Search</label>
  <input type="text" name="query" id="search" value="">
  <button>Submit</button>
</form>

<%= paginate @stills %>

<ul class="group still-list">
  <% i = 0 %>
  <% @stills.each do |still| %>
    <% i+=1 %>
  <li><a href="<%= still_url(still) %>"><img src="<%= still.image.url(:medium)%>"/></a></li>
  <% if i == 3 %>
    <br>
    <% i = 0 %>
  <% end %>
  <% end %>
</ul>
